{
	"custom": {
		"id": "custom", 
		"name": "Custom",
		"category": "",
		"default_name": "",
		"template_files": {
			"Dockerfile": {"content": "FROM scratch"}
		}
	},
	"postgres": {
		"id": "postgres", 
		"name": "PostgreSQL",
		"category": "Data Stores",
		"default_name": "postgres",
		"state_settings": {
			"ready_pattern": "ready to accept connections",
			"ready_timeout": "20s",
			"host_config": {
				"PortBindings": {
					"5432/tcp": [{"HostPort": ":0"}]
				}
			}
		},
		"template_files": {
			"Dockerfile": {"content": "FROM postgres"}
		}
	}
}