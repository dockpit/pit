auth

users /users
	given 
		mongodb: 'one user and a group'
		nsq: 'two messages'
	when GET /users
	then 200 [{"id": "32"}]
	while
		GET auth:user

user /users/:user_id
	when GET /users/32
	then 200 {"id": "32"}